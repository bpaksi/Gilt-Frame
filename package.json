{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3501",
    "build": "tsx scripts/generate-contacts.ts && next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "validate:config": "tsx scripts/validate-config.ts",
    "precommit": "pnpm db:types && pnpm lint && pnpm typecheck && pnpm validate:config && pnpm build",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:reset": "supabase db reset",
    "db:push": "supabase db push",
    "db:types": "supabase gen types typescript --local > src/lib/supabase/types.ts",
    "contacts:env": "tsx scripts/contacts-to-env.ts",
    "oracle:test": "node src/scripts/oracle-stress-test.mjs",
    "oracle:token": "node src/scripts/get-test-token.mjs",
    "tunnel": "ngrok http 3501",
    "dev:tunnel": "until curl -s http://localhost:4040/api/tunnels | grep -q public_url; do echo 'Waiting for ngrok...'; sleep 1; done && NEXT_PUBLIC_SITE_URL=$(curl -s http://localhost:4040/api/tunnels | grep -o 'https://[^\"]*\\.ngrok-free\\.[a-z]*') next dev --port 3501"
  },
  "dependencies": {
    "@supabase/ssr": "^0.8.0",
    "@supabase/supabase-js": "^2.97.0",
    "@types/bcryptjs": "^2.4.6",
    "@upstash/qstash": "^2.9.0",
    "bcryptjs": "^3.0.3",
    "next": "16.1.6",
    "qrcode.react": "^4.2.0",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "ua-parser-js": "^2.0.9"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "babel-plugin-react-compiler": "1.0.0",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "ngrok": "5.0.0-beta.2",
    "supabase": "latest",
    "tailwindcss": "^4",
    "tsx": "^4.21.0",
    "typescript": "^5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "ngrok",
      "sharp",
      "supabase",
      "unrs-resolver"
    ]
  }
}
